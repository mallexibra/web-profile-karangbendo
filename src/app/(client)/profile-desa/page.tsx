import ContainerClient from '@/components/containers/ContainerClient';

export default function ProfileDesaPage() {
  return (
    <ContainerClient>
      <p>Ini halaman profile desa</p>
    </ContainerClient>
  );
}
